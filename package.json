{
  "name": "game-live-stream-forum",
  "version": "1.0.0",
  "description": "Game Live Stream Forum",
  "author": "Chaol",
  "private": true,
  "scripts": {
    "dev": "npm run prebuild && nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "npm run prebuild && nuxt generate -c nuxt.config.generate.js",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "prebuild": "node ./build/index.js"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.0.0",
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0",
    "vue-cookies": "^1.5.13",
    "vuetify": "^1.2.4"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "eslint": "^5.0.1",
    "eslint-loader": "^2.0.0",
    "eslint-plugin-vue": "^4.0.0",
    "git-rev-sync": "^1.12.0",
    "nodemon": "^1.11.0",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2"
  }
}
